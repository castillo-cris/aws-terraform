name: Deploy Lambda and API Gateway

on:
  push:
    branches:
      - feature/backend-health

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Prepare Lambda zip
        run: |
          cd lambda
          zip -r ../lambda.zip handler.py model_temp.json model_class.json
          cd ..

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v2
        with:
          terraform_version: 1.5.7

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      - name: Terraform Init
        working-directory: infrastructure
        run: terraform init

      - name: Terraform Plan
        working-directory: infrastructure
        run: terraform plan

      - name: Terraform Apply
        working-directory: infrastructure
        run: terraform apply -auto-approve

      - name: Get API endpoint
        id: api
        working-directory: infrastructure
        run: |
          echo "endpoint=$(terraform output -raw http_api_endpoint)" >> $GITHUB_ENV

      - name: Test HTTP inference
        run: |
          curl -s -X POST "$endpoint/infer" \
            -H "Content-Type: application/json" \
            -d '{"city":"Medellín","features":{"temp":21.32,"hum":73,"pres":1013,"wind":2.06}}' | tee response_http.json

      - name: Show response
        run: cat response_http.json